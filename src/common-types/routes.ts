export enum RouteEnum {
  main = '/',
  user = '/user',
}

export enum UrlsEnum {
  users = '/search/users',
  user = '/users',
  repositoryUser = '/repos',
  searchRepository = '/search/repositories',
}
